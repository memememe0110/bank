# Wallet Mock v66

お気に入り登録を「明細行ごと」に変更しました。

- ☆を押した明細だけ★になる
- 別の明細を開くと、その明細自身のお気に入り状態を表示
- お気に入り解除もその行だけ
- 同じような明細が複数あっても、行ごとに区別
- 状態は端末内に保存されるため、ページを開き直しても維持

取引明細のコンパクト表示(v65)はそのまま維持しています。
明細データや残高計算は変更していません。
