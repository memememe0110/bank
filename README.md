# Wallet Mock v67

Walletの残高が0円になる不具合を修正しました。

- Walletの残高は `account-data.js` の丸善ジュンク堂支店明細から自動計算
- `openingBalance + credit - debit` の計算結果を表示
- 初期状態はこれまで通り残高非表示
- 「残高を表示」を押した時だけ自動計算した金額を表示
- 以前の `¥0` 固定処理は、この更新処理で無効化
- 明細データや残高データ自体は変更していません
